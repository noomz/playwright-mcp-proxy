---
phase: 01-dependency-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - playwright_mcp_proxy/config.py
  - .mcp.json
  - uv.lock
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03

must_haves:
  truths:
    - "pydantic-settings is an explicit dependency in pyproject.toml"
    - "All dependency version floors match or exceed versions resolved in uv.lock"
    - "@playwright/mcp is pinned to 0.0.68 in both config.py and .mcp.json"
    - "Existing test suite passes with zero regressions"
  artifacts:
    - path: "pyproject.toml"
      provides: "Correct dependency declarations with version floors"
      contains: "pydantic-settings>=2.11.0"
    - path: "playwright_mcp_proxy/config.py"
      provides: "Pinned Playwright MCP version in subprocess args"
      contains: "@playwright/mcp@0.0.68"
    - path: ".mcp.json"
      provides: "Pinned Playwright MCP version for Claude Desktop config"
      contains: "@playwright/mcp@0.0.68"
    - path: "uv.lock"
      provides: "Regenerated lockfile consistent with updated pyproject.toml"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock regeneration"
      pattern: "pydantic-settings"
    - from: "playwright_mcp_proxy/config.py"
      to: "playwright_mcp_proxy/server/playwright_manager.py"
      via: "settings.playwright_args consumed at subprocess spawn"
      pattern: "@playwright/mcp@0\\.0\\.68"
---

<objective>
Update all dependency declarations and pin the Playwright MCP subprocess version.

Purpose: Eliminate three sources of build fragility — a missing explicit dependency (pydantic-settings), stale version floors that allow untested older versions, and an unpinned npm package that can silently change between runs.

Output: Updated pyproject.toml, config.py, .mcp.json, regenerated uv.lock, passing test suite.
</objective>

<execution_context>
@/Users/noomz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/noomz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-dependency-hygiene/01-RESEARCH.md

@pyproject.toml
@playwright_mcp_proxy/config.py
@.mcp.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pyproject.toml dependencies and pin @playwright/mcp</name>
  <files>pyproject.toml, playwright_mcp_proxy/config.py, .mcp.json</files>
  <action>
Edit pyproject.toml `[project] dependencies` to:
```
"fastapi>=0.119.0",
"uvicorn>=0.34.0",
"aiosqlite>=0.21.0",
"mcp>=1.15.0",
"httpx>=0.28.0",
"pydantic>=2.10.0",
"pydantic-settings>=2.11.0",
"python-dotenv>=1.0.0",
"psutil>=6.1.0",
```

Key changes:
- ADD `pydantic-settings>=2.11.0` (DEPS-01 — was missing, imported directly in config.py but only resolved transitively via mcp)
- BUMP fastapi, uvicorn, aiosqlite, mcp, httpx, pydantic floors to match resolved versions in uv.lock (DEPS-02)
- KEEP python-dotenv and psutil at current floors (stable APIs, no benefit to bumping)

Edit `[project.optional-dependencies] dev` to:
```
"pytest>=8.3.0",
"pytest-asyncio>=1.0.0",
"pytest-cov>=6.0.0",
"ruff>=0.9.0",
```

Key changes:
- BUMP pytest-asyncio from >=0.24.0 to >=1.0.0 (major version jump; project uses asyncio_mode=auto which is compatible)
- BUMP ruff from >=0.7.0 to >=0.9.0 (conservative bump, resolved is 0.14.1)
- KEEP pytest and pytest-cov (already close to resolved)

Edit playwright_mcp_proxy/config.py line 29 — change `playwright_args` default:
- FROM: `default=["@playwright/mcp@latest"]`
- TO: `default=["@playwright/mcp@0.0.68"]`

Also migrate the deprecated `class Config` inner class to the pydantic-settings 2.x preferred pattern. Replace:
```python
class Config:
    """Pydantic config."""
    env_prefix = "PLAYWRIGHT_PROXY_"
    env_file = ".env"
    env_file_encoding = "utf-8"
```
With:
```python
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_prefix="PLAYWRIGHT_PROXY_",
        env_file=".env",
        env_file_encoding="utf-8",
    )
    # ... rest of fields unchanged
```
Remove the inner `class Config` block entirely. Add `SettingsConfigDict` to the import from pydantic_settings.

Edit .mcp.json — change args:
- FROM: `"@playwright/mcp@latest"`
- TO: `"@playwright/mcp@0.0.68"`

After all edits, verify no remaining `@latest` references:
```bash
rg '@playwright/mcp@latest' .
```
This must return zero results.
  </action>
  <verify>
    <automated>cd /Users/noomz/Projects/Opensources/playwright-mcp-proxy && rg '@playwright/mcp@latest' . ; echo "exit:$?" && rg 'pydantic-settings' pyproject.toml && rg '@playwright/mcp@0.0.68' playwright_mcp_proxy/config.py .mcp.json</automated>
  </verify>
  <done>
- pyproject.toml has pydantic-settings>=2.11.0 in dependencies
- All version floors bumped per research table
- @playwright/mcp@latest replaced with @0.0.68 in config.py and .mcp.json
- Config class migrated to SettingsConfigDict
- Zero grep hits for @latest
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate lockfile, install, and verify test suite</name>
  <files>uv.lock</files>
  <action>
Run the following commands in sequence:

1. Regenerate the lockfile:
```bash
uv lock
```
If this fails with a resolution conflict, check which package is the culprit with `uv tree` and adjust the conflicting floor downward to the nearest compatible version. The most likely conflict candidate is `mcp>=1.15.0` (if a transitive dep caps it).

2. Install in editable mode with dev deps:
```bash
uv pip install -e ".[dev]"
```

3. Verify pydantic-settings is importable as an explicit dep:
```bash
uv run python -c "from pydantic_settings import BaseSettings, SettingsConfigDict; print('pydantic-settings OK')"
```

4. Verify Settings class loads without deprecation warnings:
```bash
uv run python -c "from playwright_mcp_proxy.config import settings; print(f'Settings loaded, port={settings.server_port}, args={settings.playwright_args}')"
```
Expected output should show `args=['@playwright/mcp@0.0.68']`.

5. Run ruff to check for any new lint violations from version bumps:
```bash
uv run ruff check .
```
Fix any new violations if they appear. Do NOT add them to ignore list without understanding them.

6. Run the full test suite:
```bash
uv run pytest -v
```
All 14+ tests must pass. If any async fixture errors appear (from pytest-asyncio 1.x), investigate — the project uses `asyncio_mode = "auto"` which should be compatible. Look for `ScopeMismatch` or `FixtureError` specifically.
  </action>
  <verify>
    <automated>cd /Users/noomz/Projects/Opensources/playwright-mcp-proxy && uv run pytest -v</automated>
  </verify>
  <done>
- uv lock succeeds without resolution conflicts
- uv pip install -e ".[dev]" succeeds
- pydantic-settings imports cleanly
- Settings class loads with pinned playwright_args
- ruff check passes
- Full test suite passes (14+ tests, 0 failures)
  </done>
</task>

</tasks>

<verification>
All three requirements verified:
- DEPS-01: `rg 'pydantic-settings' pyproject.toml` returns a hit
- DEPS-02: `uv lock` succeeds and `uv run pytest -v` passes (floors are compatible with resolved versions)
- DEPS-03: `rg '@playwright/mcp@latest' .` returns zero results AND `rg '@playwright/mcp@0.0.68' playwright_mcp_proxy/config.py .mcp.json` returns hits in both files
</verification>

<success_criteria>
1. pydantic-settings is listed in pyproject.toml [project] dependencies
2. All runtime dependency floors are >= the versions in the research table
3. @playwright/mcp@0.0.68 appears in both config.py and .mcp.json; @latest appears nowhere
4. uv lock and uv pip install -e ".[dev]" both succeed
5. uv run pytest -v passes all tests with zero failures
6. uv run ruff check . passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-dependency-hygiene/01-01-SUMMARY.md`
</output>
