---
phase: 09-fix-skill-accuracy-and-tracking-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/playwright-proxy/SKILL.md
  - .claude/skills/playwright-proxy/SKILL.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SKIL-01
  - SKIL-02
  - SKIL-03
  - SKIL-04
  - SKIL-05
  - SKIL-06
  - CHRM-01
  - CHRM-02
  - CHRM-03

must_haves:
  truths:
    - "SKILL.md tool reference table lists exactly 9 tools matching mcp_server.py"
    - "No mention of list_sessions or resume_session anywhere in SKILL.md"
    - "Session Recovery section is removed from SKILL.md"
    - "All 27 requirements in REQUIREMENTS.md are checked [x]"
    - "Traceability table shows Complete for all 27 requirements"
  artifacts:
    - path: "~/.claude/skills/playwright-proxy/SKILL.md"
      provides: "Accurate skill documentation with 9 tools"
      contains: "get_console_content"
    - path: ".claude/skills/playwright-proxy/SKILL.md"
      provides: "Project-local copy of accurate skill documentation"
      contains: "get_console_content"
    - path: ".planning/REQUIREMENTS.md"
      provides: "All requirements marked complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 progress updated"
  key_links:
    - from: "~/.claude/skills/playwright-proxy/SKILL.md"
      to: "playwright_mcp_proxy/client/mcp_server.py"
      via: "tool names match TOOLS list"
      pattern: "9 tool rows in table"
---

<objective>
Fix SKILL.md accuracy by removing 2 phantom MCP tools (list_sessions, resume_session) and their Session Recovery section, then close all requirement tracking gaps by marking SKIL-01..06 and CHRM-01..03 as complete in REQUIREMENTS.md.

Purpose: Close the v1.0 milestone audit gaps so all 27 requirements show Complete status and SKILL.md only documents tools that actually exist.
Output: Updated SKILL.md (both copies), REQUIREMENTS.md, and ROADMAP.md
</objective>

<execution_context>
@/Users/noomz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/noomz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-fix-skill-accuracy-and-tracking-gaps/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SKILL.md — remove phantom tools and Session Recovery section</name>
  <files>~/.claude/skills/playwright-proxy/SKILL.md, .claude/skills/playwright-proxy/SKILL.md</files>
  <action>
Edit `~/.claude/skills/playwright-proxy/SKILL.md` to make these changes:

1. **Remove 2 phantom tool rows from the Tool Reference table** (currently lines 65-66):
   - Delete: `| \`list_sessions\` | \`state\` (optional: active/closed/error/recoverable/stale) | list of sessions |`
   - Delete: `| \`resume_session\` | \`session_id\` (required) | resumed session confirmation |`
   - The table should have exactly 9 data rows after this (create_new_session through get_console_content).

2. **Remove the "Session Recovery" section** (currently lines 115-122):
   - Delete the entire block from `## Session Recovery` through `...Once closed, they cannot be resumed.`

3. **Simplify the session states line** (currently line 113 in the Management CLI section):
   - Change: `Session states: \`active\`, \`closed\`, \`error\`, \`recoverable\`, \`stale\`. The \`clear\` command defaults to clearing \`closed\` sessions.`
   - To: `Session states: \`active\`, \`closed\`, \`error\`. The \`clear\` command defaults to clearing \`closed\` sessions.`
   - Reason: `recoverable` and `stale` were only meaningful for the removed list_sessions/resume_session tools. The ctl commands only use active/closed/error.

4. **Copy the updated file** to the project-local location:
   - Copy `~/.claude/skills/playwright-proxy/SKILL.md` to `.claude/skills/playwright-proxy/SKILL.md`
   - Both files must be identical after this task.

Do NOT add any new tools or sections. Do NOT edit mcp_server.py or any Python files.
  </action>
  <verify>
    <automated>grep -c "list_sessions\|resume_session" ~/.claude/skills/playwright-proxy/SKILL.md | grep -q "^0$" && echo "PASS: no phantom tools" || echo "FAIL: phantom tools remain"; grep -c "^| \`" ~/.claude/skills/playwright-proxy/SKILL.md | grep -q "^9$" && echo "PASS: exactly 9 tools" || echo "FAIL: wrong tool count"; grep -c "Session Recovery" ~/.claude/skills/playwright-proxy/SKILL.md | grep -q "^0$" && echo "PASS: section removed" || echo "FAIL: section remains"; diff ~/.claude/skills/playwright-proxy/SKILL.md .claude/skills/playwright-proxy/SKILL.md && echo "PASS: copies identical" || echo "FAIL: copies differ"</automated>
  </verify>
  <done>SKILL.md contains exactly 9 tools in the reference table, no mention of list_sessions or resume_session anywhere, Session Recovery section removed, session states line simplified, both file copies identical</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md checkboxes and traceability, update ROADMAP.md progress</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Edit `.planning/REQUIREMENTS.md`:

1. **Check 6 SKIL requirement boxes** — change `- [ ]` to `- [x]` for:
   - SKIL-01, SKIL-02, SKIL-03, SKIL-04, SKIL-05, SKIL-06

2. **Check 3 CHRM requirement boxes** — change `- [ ]` to `- [x]` for:
   - CHRM-01, CHRM-02, CHRM-03

3. **Update 9 traceability table rows** — in the `## Traceability` table at the bottom, change "Pending" to "Complete" for:
   - SKIL-01 through SKIL-06
   - CHRM-01 through CHRM-03

After edits, all 27 requirements should show `[x]` and all 27 traceability rows should show "Complete".

Edit `.planning/ROADMAP.md`:

4. **Update Phase 9 progress row** in the Progress table:
   - Change: `| 9. Fix SKILL.md & Tracking | 0/1 | Planned | - |`
   - To: `| 9. Fix SKILL.md & Tracking | 1/1 | Complete | 2026-03-13 |`

5. **Update Phase 9 plans count**:
   - Change: `**Plans:** 0 plans`
   - To: `**Plans:** 1 plan`

Do NOT change any other phases or requirements.
  </action>
  <verify>
    <automated>grep -E "\[ \] \*\*(SKIL|CHRM)" .planning/REQUIREMENTS.md | wc -l | grep -q "^0$" && echo "PASS: all checked" || echo "FAIL: unchecked remain"; grep "Pending" .planning/REQUIREMENTS.md | wc -l | grep -q "^0$" && echo "PASS: no Pending" || echo "FAIL: Pending remains"; grep -E "\[x\] \*\*(SKIL|CHRM)" .planning/REQUIREMENTS.md | wc -l | tr -d ' ' | grep -q "^9$" && echo "PASS: 9 checked" || echo "FAIL: wrong count"; uv run pytest tests/test_chrome_comparison.py -m chrome_comparison -q 2>/dev/null && echo "PASS: chrome tests pass" || echo "WARN: chrome tests not run (optional)"</automated>
  </verify>
  <done>All 27 requirements checked in REQUIREMENTS.md, all 27 traceability rows show Complete, ROADMAP.md Phase 9 progress shows 1/1 Complete</done>
</task>

</tasks>

<verification>
1. `grep -c "list_sessions\|resume_session" ~/.claude/skills/playwright-proxy/SKILL.md` returns 0
2. `grep -c "^| \`" ~/.claude/skills/playwright-proxy/SKILL.md` returns 9
3. `grep -E "\[ \] \*\*" .planning/REQUIREMENTS.md` returns empty (no unchecked requirements)
4. `grep "Pending" .planning/REQUIREMENTS.md` returns empty (no Pending in traceability)
5. `uv run pytest -q` — full test suite passes (no regressions)
</verification>

<success_criteria>
- SKILL.md tool reference table has exactly 9 rows matching the 9 tools in mcp_server.py
- Zero occurrences of "list_sessions" or "resume_session" in SKILL.md
- All 27 requirement checkboxes show [x] in REQUIREMENTS.md
- All 27 traceability rows show Complete in REQUIREMENTS.md
- ROADMAP.md Phase 9 shows 1/1 Complete
- Both SKILL.md copies (home and project) are identical
</success_criteria>

<output>
After completion, create `.planning/phases/09-fix-skill-accuracy-and-tracking-gaps/09-01-SUMMARY.md`
</output>
